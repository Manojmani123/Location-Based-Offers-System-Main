Location-Based-Offers-System-Main
Overview
The Location-Based Offer System is a web application that delivers personalized offers to users based on their real-time location. It is designed with security as a core principle, aligned with NIST SP 800-218 (SSDF).

Features
User location detection
Nearby offers and deals display
Business offer management
Secure user authentication
Data protection and validation
Scalable backend architecture
Tech Stack
Backend: json,javascript
Frontend: HTML, CSS, JavaScript
Database:
Version Control: Git
How to run?
download code file?
open in vs code -run backend, run admin, run superadmin, run customer -commands 1.open terminal -cd backend -npm i -npm run dev 2.open terminal -cd admin -npm i -npm run dev 3.open terminal -cd customer -npm i -npm run dev 4.open terminal -cd superadmin -npm i -npm run dev
